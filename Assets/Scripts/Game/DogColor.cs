using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DogColor : AgentBehaviour
{
    public float R; 
    public float G;
    public float B;
    // Start is called before the first frame update
    void Start()
    {
      //  SetVisualEffect((long)0, (long)R, (long)G, (long)B, (long)10);
    }
}
